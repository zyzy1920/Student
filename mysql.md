# 架构

### 连接层

#### 处理客户端的连接

客户端和服务端建立tcp连接

#### 身份验证

验证用户名密码并获取用户权限，权限逻辑判断都基于此时读取到的权限

### 服务层

#### 缓存

key是sql语句，value是执行结果。更新表时，该表的所有缓存都失效。
mysql8.0中将该缓存功能取消了

#### 解析sql

检查语法，构建语法树

#### 执行sql

##### 预处理

1. 判断表和字段是否存在
2. 将select \* 中的 \* 替换为具体的字段名

##### 优化器

确定执行计划，主要是看看走哪个索引

##### 执行器

根据执行计划执行 SQL，从存储引擎读取记录，返回给客户端

### 存储引擎层

#### 文件结构

表空间 (TableSpace)：包含多个段。

##### 段 (Segment)

- 索引段：存放 B + 树的非叶子节点的区的集合。 每个索引对应一个索引段。
- 数据段：存放 B + 树的叶子节点的区的集合。 每个索引对应一个数据段。 为什么这样？  因为不同的索引对应的数据是不同的，比如根据name字段建立的索引和age字段索引的叶子节点数据是不同的。
- 回滚段：存放回滚数据的区的集合。 默认128个回滚段，每个段1024个槽，每个槽对应1个事务，所以最大支持同时大约13万个事务，超出就报错

##### 区 (Extent)：默认 1MB（连续的 64 个页）

当存在多个数据页时，可能不用页逻辑上是相邻的，但是物理上不是相邻的，随造成随机IO。为了让逻辑相邻的页在物理上也相邻，所以分配空间时按照区来分配，这样相邻的页在物理上也相邻。

##### 页 (Page)：磁盘与内存交互的最小单位，默认 16KB

INNODB的数据是以页为单位来读写的。也就是说，当需要读一条记录的时候，并不是将这个行记录从磁盘读出来，而是以页为单位，将其整体读入内存。

##### 行 (Row)：数据最终以“行”的形式存储在页中

每行记录根据不同的行格式，有不同的存储结构。从 MySQL5.7 版本之后，默认使用 Dynamic 行格式。Compact和Dynamic很相似，先学一下Compact。

#### Compact行格式

##### 记录的额外信息

###### 变长字段长度列表

记录如 VARCHAR、TEXT 这种变长字段实际占用的字节数。它是逆序存放的。
为什么逆序存放呢？
字段长度数据距离真实数据的距离更近，读取速度更快

###### NULL值列表

如果存在允许 NULL 值的列，则每个列对应一个二进制位（bit），二进制位按照列的顺序逆序排列。

- 二进制位的值为1时，代表该列的值为NULL。
- 二进制位的值为0时，代表该列的值不为NULL。

###### 记录头信息

- delete\_mask:标识此条数据是否被删除。从这里可以知道，我们执行detele删除记录的时候，并不会真正的删除记录，只是将这个记录的delete\_mask标记为1。
- nextrecord:下一条记录的位置。从这里可以知道，记录与记录之间是通过链表组织的。在前面我也提到了，指向的是下一条记录的「记录头信息」和「真实数据」之间的位置，这样的好处是向左读就是记录头信息，向右读就是真实数据，比较方便。
- record\_type:表示当前记录的类型，0表示普通记录，1表示B+树非叶子节点记录，2表示最小记录，3表示最大记录

##### 记录的真实信息

###### 隐藏字段

- row\_id
  如果有主键或者唯一约束，就没有row\_id字段。否则，就会自动生成一个row\_id字段，6字节。
- trx\_id
  事务id，表示这个数据是由哪个事务生成的。 trx\_id是必需的，占用 6 个字节。
  相当于是记录一下哪个事务修改了这行数据。
- roll\_pointer
  这条记录上一个版本的指针。roll\_pointer 是必需的，占用 7 个字节。
  roll\_pointer 本质上是一个指向 Undo Log（回滚日志） 的指针。
  相当于是记录一下这行数据之前是什么样。

##### varchar(n) 中 n 最大取值为多少

n表示的是字节数
一行记录最大存储65535字节数据 uint16
n = 65535- null值列表占用的字节数 - 真实数据的字节数

##### 行溢出

- Compact 将溢出的数据放到溢出页中，在真实数据中存储溢出页的地址。
- Dynamic 是采用完全溢出的策略 将溢出的数据全部放到溢出页中，在真实数据中只存储溢出页的地址。

# 索引

## 概述

MySQL存储引擎有 MylSAM、InnoDB、Memory，其中InnoDB 是在MySQL 5.5之后成为默认的存储引擎。

## 主键索引

#### 创建

- 如果有主键，默认会使用主键作为聚簇索引的索引键(key);
- 如果没有主键，就选择第一个不包含NULL值的唯一列作为聚簇索引的索引键(key);
- 在上面两个都没有的情况下，InnoDB将自动生成一个隐式自增id列作为聚簇索引的索引键(key);

叶子节点存放的是数据，非叶子节点存放索引键。
叶子节点之间是通过双向链表连接起来的。 有利于范围查询

#### 索引覆盖

如果查询的数据是id，那么就不需要再回表了，这就是索引覆盖

## 普通索引

叶子节点存放的是主键值，非叶子节点存放索引键。

#### 回表

例如： 根据name查询user表的所有数据，需要先根据name索引找到对应的叶子节点，拿到叶子节点中的主键值去主键索引中查询完整数据。

## 唯一索引

唯一索引建立在UNIQUE字段上的索引，一张表可以有多个唯一索引，索引列的值必须唯一，但是允许有
空值。 可以同时有多个null值。

<br />

&#x20;     ` CREATE TABLE table name
UNIQUE KEY(index_column_1,index_column_2,...)`

`CREATE UNIQUE INDEX index_nameON table_name(index_column_1,index_column_2,...);`

## 前缀索引

前缀索引是指对字符类型字段的前几个字符建立的索引，而不是在整个字段上建立的索引，前缀索引可以建立在字段类型为char、varchar、binary（二进制字符串）、varbinary（动态的二进制binary） 的列上

## 联合索引

假设我们建立一个联合索引：`INDEX(col1, col2, col3)`。

- 首先按照 `col1` 进行排序。
- 在 `col1` 相同的情况下，再按照 `col2` 进行排序。
- 在 `col2` 也相同的情况下，最后按照 `col3` 排序。

从全局看，只有第一列 col1 是绝对有序的。而 col2 和 col3 只有在前面的列值相同时，局部才是有序的。

遵循最左前缀原则
联合索引的中的字段是顺序是很重要的
sql中 where条件里字段顺序顺序无所谓，服务层会优化的
遇到范围查询 会停止匹配
例子：
`Q1: select*from ttable where a>1 and b=2，联合索引(a,b)哪一个字段用到了联合索引的B+Tree?`
答案：a，因为a是范围查询，到a就会停止继续匹配

`Q2: select*fromt_table where a>=1 and b=2，联合索引(a,b)哪一个字段用到了联合索引的B+Tree?`
答案：a、b都走索引了。因为a=1时，b是有序的，所以b也可以走索引。

`Q3: SELECT*FROM t_table WHERE a BETWEEN 2 AND 8 AND b=2，联合索引I (a, b) 哪一个字段用到了联合索引的B+Tree?`
答案：a、b都走索引了。因为a=2时，b是有序的，所以b也可以走索引。

`Q4: SELECT * FROM t_user WHERE name like ‘j%' and age = 22, 联合索引 (name, age) 哪个字段用到了联合索引的B+Tree?`
答案：name和age，当name=”jxx“时，age是有序的，所以age也可以走索引。

**索引下推**
例如 `select * from table where a > 1 and b = 2` 执行这个sql时，找到第一个满足条件的主键值后，需要判断b是否=2，那么在联合索引中判断还是主键索引呢？
mysql5.6之前要走主键索引，server会找到所有符合条件的主键id，然后回表拿整行数据
mysql5.6开始存储引擎过滤掉b!=2的数据，=2才回表
在联合索引遍历的过程中，直接剔除不符合条件的数据，减少回表次数

## 什么时候适用索引？

- 经常用于 WHERE查询条件的字段，这样能够提高整个表的查询速度，如果查询条件不是一个字段，可以建立联合索引。
- ORDER BY、GROUP BY 的字段，这样在查询的时候就不需要再去做一次排序了，因为我们经常用于GROUPBY和都已经知道了建立索引之后在B+Tree中的记录都是排序好的。

## 什么时候不适用索引？

- 经常更新的字段
- 不作为查询条件的字段
- 区分度低的字段，例如性别

## 索引优化

### 前缀索引优化

对一些前缀有很好区分度的数据，对前缀建立索引
**优点**

- 减小索引体积，节约空间
- 提升索引速度
  **缺点**
- 无法使用“覆盖索引”
- 无法用于排序 (Order By) 和 分组 (Group By)

例如 name，邮箱

### 覆盖索引优化

通过建立合适的普通索引/联合索引，使查询时尽量直接命中要查询的数据，避免回表。

### 自增主键

#### 避免随机写入

InnoDB 的聚簇索引（主键索引）是按主键顺序物理存储在磁盘上的。
自增主键（顺序）：每次插入新记录，数据都会追加在当前 B+ Tree 的末尾。这就像在笔记本上按页码往后写，不需要移动之前写好的内容。这种顺序 I/O 的速度极快。
非自增/随机主键（如 UUID）：由于主键值是随机的，新数据可能会被插入到 B+ Tree 中间的任何位置。
为了插入这条数据，InnoDB 必须先找到对应的位置，然后将旧数据挪开。
如果目标页已经满了，就会触发页分裂（Page Split）。

#### 避免页分裂与碎片

当一个 16KB 的数据页满了，却还要往中间插入数据时，InnoDB 必须申请一个新页，并把原页中 50% 的数据迁移过去。

- 性能抖动：频繁的页分裂会导致大量磁盘 I/O 和内存操作。
- 碎片化：物理上的存储不再连续，导致后续的全表扫描或范围查询变慢。
  如果是自增主键，页满后，创建新的页即可

#### 节约二级索引的空间

二级索引的叶子节点就是主键，主键越小，二级索引的叶子节点就越小。

### 索引设置为NOT NULL

-索引列计算null值时，更加复杂
-null值占用物理空间，因为有null值列表，所以就算只有一个null值列，至少也会占用一个字节的空间。

## 索引失效

- 在 WHERE 子句的索引列上使用函数。因为b+tree中保存的是原始数据，使用函数后的值和原始值无法匹配，所以必定失效
- 类型不匹配导致类型转换。字段是char类型，查询时没带引号，mysql会将字符串转换为数字进行计算，相当于是加一个cast()函数
- 模糊查询中，左边是百分号。b+tree是从左到右进行排序的，如果左边不确定，就不知道从树的哪里开始找
- 联合索引中，不符合最左匹配原则。最左边的字段匹配不到，其余的肯定是无序的。
- OR 连接中存在非索引列

## 从数据页的微观视角来看b+树

### 1. 物理存储单元：16KB 的页 (Page)

InnoDB 存储数据的基本单位是页，默认大小为 **16KB**。B+ 树的每一个节点（无论是根节点、非叶子节点还是叶子节点）本质上都是一个数据页。

- **页间组织**：页与页之间通过文件头（File Header）中的两个指针连接，构成一个**双向链表**。
- **逻辑连续**：这种链表结构使得数据页在物理上可以不连续，但在逻辑上保持有序。
- **内部组成**：一个数据页由文件头、页头、最大/最小记录、用户记录、空闲空间、页目录和文件尾七个部分组成。

***

### 2. 页内检索核心：页目录 (Page Directory)

为了提高在单个页内数千条记录中查找的效率，InnoDB 引入了页目录机制：

- **记录分组**：页内记录（User Records）按主键顺序组成单向链表。这些记录被划分为若干组。
- **槽 (Slot)**：页目录由多个槽组成，每个槽指向其对应分组中主键值最大的那条记录。
- **二分查找**：在页内查询时，通过对槽进行**二分法**快速定位目标记录所属的分组，随后在分组内（通常只有 4-8 条记录）进行少量的顺序遍历。

***

### 3. B+ 树的层级结构

当数据量增大需要多个数据页时，B+ 树通过分层管理这些页：

- **非叶子节点**：仅存放“目录项”，用于记录下层页面的最小主键值与页号，起到索引导航作用。
- **叶子节点**：处于最底层，根据索引类型存储具体内容：
  - **聚簇索引**：存储完整的用户行记录。
  - **二级索引**：仅存储索引键值和对应的主键值。

***

### 4. 查询的本质：两次二分定位

在 B+ 树中寻找一条记录，本质上经历了两个阶段的加速：

1. **跨页定位**：从根节点开始，通过二分法在各层非叶子节点中快速找到目标记录所在的**数据页**。
2. **页内定位**：进入目标页后，再次利用页目录的二分法快速定位到**记录分组**，最终找到具体数据。

***

### 5. 关键术语总结

- **聚簇索引**：一张表只有一个，因数据物理上只保存一份，通常基于主键构建。
- **回表**：使用二级索引查询时，若所需字段不在索引中，需先找主键值，再回到聚簇索引查找完整行记录。
- **索引覆盖**：若二级索引已包含查询所需的全部字段，则无需回表，只需查一棵 B+ 树。

<br />

## innodb为什么选择b+树
 1. 非叶子节点不存储数据，只存储索引键值和下一级节点的指针，所以树的层数更少，每个数据页能存储更多的记录，减少磁盘io，提升查询效率
 2. 叶子节点是通过双向链表连接，且有序的，有利于范围查询。
 3. 查询性能比较稳定，因为所有数据都在叶子节点才能找到。

## 数据超过2000w行后，性能会下降
1. 第一层 根节点 一个数据页，16kb，假设页头1kb，每个索引键加指针15字节（索引键8字节，指针6字节），那么15kb能存约1070条记录
2. 第二层 非叶子节点 1070*1070=1144900
3. 第三层 假设每条记录的真实数据是1kb，那么一个数据页就是大概15条数据 15*1144900  约1700w
  说是2000w也大概问题

三层对应大概2000w条记录。
超过3层后，意味着每一次查询都要多一次IO。


## count关键字
count(*)、count(1)、count(字段名) 有什么区别呢
- count(*)、count(1) 统计所有记录的条数，包括null值的记录
- count(字段名) 会统计字段名不为null的记录数

速度对比
- count（1）和count(*) 优化器自动选择开销最小的二级索引，不取值、不判空。速度最快
- count(主键)，因为主键肯定不为空，所以绝大多数情况下，count(主键)和count(*)是相等的。如果有二级索引，则会优化为走开销最小的二级索引
- count(二级索引列) 全索引扫描，只遍历叶子节点就行，叶子节点存的是主键id和索引列的值，体积小，每个页更存更多的数据，这样io次数更少。
- count(非索引字段) 全表扫描，遍历主键索引的所有叶子节点，主键索引的叶子节点放所有数据，所以比较大，那么一个页放的数据就更少，io次数就更多。

**如果没有二级索引那么只能走主键索引，此时只能扫描全表了**
所以一定要在表中加二级索引，最好是在最小的字段中加二级索引


# 事务
## 事务的特性
 - 原子性 事务是一个不可分割的工作单位，要么全部命令执行成功，要么全部失败。
 - 一致性 一致性就是保证系统从一个平衡状态，变成另一个平衡状态。比如运动会消耗能量 这是一致性的；运动不消耗能量 这就是状态不一致的。业务层面要保证一致性，
    a给b转账100，那么a要-100，b要+100，才能保证一致性。数据库层面，我们设置了id为主键索引，数据库就要保证id肯定是唯一且非空的。
 - 隔离性 当多个事务同时操作同一个数据库时，如何保证它们互不干扰，就像每个事务都在独立使用数据库一样。
 - 持久性 事务一旦提交成功，其对数据库的修改就会永久保存。


- 持久性是通过 redo log(重做日志)来保证的;
- 原子性是通过undolog(回滚日志)来保证的;
- 隔离性是通过MVCC(多版本并发控制)或锁机制来保证的;
- 一致性则是通过持久性+原子性+隔离性来保证;


并行事务会遇到的问题
 - 脏读 一个事务读取到了另一个事务未提交的内容
 - 不可重复读 一个事务内，多个事务读取同一条记录，前后结果不一致
 - 幻读 一个事务内，同一个条件，多次查询，前后查到的记录数量不一致